import React from 'react'
import Link from '@/components/ui/link'

/**
 * Resusable component that links to a list. Displays the list title, author, and when it was added to the website.

 */
const ListLink = () => {
  return <div></div>
}

export default ListLink
